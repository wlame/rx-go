# Pinned dependencies for building the rx documentation site.
#
# Keep these in sync with mkdocs.yml. CI installs this file verbatim so
# local builds and GitHub Pages builds produce identical output.
#
# Update cadence:
#   - Bump mkdocs-material on minor releases (monthly-ish).
#   - Re-pin everything whenever you add a new plugin or extension.

# pymdown-extensions must be >=10.12 when Python-Markdown is >=3.10 —
# earlier pymdown versions don't register their fence/priority hooks
# correctly against the newer Markdown API, which makes ``` code fences
# render as raw text (comments get parsed as ATX headings, etc.).
mkdocs==1.6.1
mkdocs-material==9.5.44
mkdocs-material-extensions==1.3.1
mkdocs-git-revision-date-localized-plugin==1.2.9
Markdown==3.10.2
pymdown-extensions==10.21.2
